import styled, { css } from 'styled-components';

export const ModalRoutineWorkoutContainer = styled.div`
  ${props => {
    const { theme } = props;
    return css``;
  }}
`;

export const ModalWorkoutPillsContainer = styled.div`
  ${props => {
    const { theme } = props;
    return css`
      margin: 15px 0px;
      width: 100%;
      display: flex;
      flex-wrap: wrap;
    `;
  }}
`;
